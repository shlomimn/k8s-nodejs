apiVersion: v1
kind: Service
metadata:
  labels:
    app: nodejs-deploy
  name: nodejs-deploy
  namespace: api-servers
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 8000
  selector:
    app: nodejs-deploy
  type: LoadBalancer

